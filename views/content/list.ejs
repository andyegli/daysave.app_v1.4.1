<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Content Management - DaySave</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">


  <style>
    .content-card {
      transition: box-shadow 0.2s;
      border-radius: 1rem;
      overflow: hidden;
      min-height: 180px;
      background: #fff;
    }
    .content-card:hover {
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
    }
    .content-card .card-title {
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      line-height: 1.3;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      word-break: break-word;
      max-width: 100%;
      margin-right: 8px;
    }
    .content-card .card-title:hover {
      color: #0d6efd;
    }
    .content-card .card-text {
      font-size: 0.95rem;
    }
    .content-card .badge {
      font-size: 0.85rem;
      margin-right: 0.2rem;
    }
    
    /* Compact button styling */
    .content-card .btn-sm {
      padding: 0.2rem 0.4rem;
      font-size: 0.8rem;
      border-width: 1px;
      min-width: 28px;
      height: 28px;
    }
    
    @media (max-width: 768px) {
      .content-card .card-title { 
        -webkit-line-clamp: 3;
        font-size: 1rem;
        margin-right: 4px;
      }
      .content-card { min-height: 140px; }
      .content-card .btn-sm {
        min-width: 24px;
        height: 24px;
        padding: 0.1rem 0.3rem;
      }
    }
    /* Enhanced Thumbnail Container - Fixed Aspect Ratio Without Stretching */
    .content-card .thumbnail-container {
      width: 90px;
      max-width: 90px;
      min-width: 90px;
      height: 90px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #f8f9fa;
      border-radius: 8px;
      overflow: hidden;
      position: relative;
    }
    
    .content-card .thumbnail-container img {
      max-width: 100%;
      max-height: 100%;
      width: auto;
      height: auto;
      object-fit: contain;
      object-position: center;
      border-radius: 6px;
      transition: transform 0.2s ease;
      display: block;
    }
    
    .content-card .thumbnail-container:hover img {
      transform: scale(1.02);
    }
    
    .content-card .thumbnail-container .fallback-icon {
      font-size: 2rem;
      opacity: 0.7;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    /* Video thumbnails - use cover to fill container nicely */
    .content-card .thumbnail-container.video-thumb img {
      object-fit: cover;
      width: 100%;
      height: 100%;
    }
    
    /* Image thumbnails - use contain to prevent stretching */
    .content-card .thumbnail-container.image-thumb img {
      object-fit: contain;
      background: #fff;
    }
    
    @media (max-width: 768px) {
      .content-card .thumbnail-container {
        width: 70px;
        height: 70px;
        min-width: 70px;
        max-width: 70px;
      }
      
      .content-card .thumbnail-container .fallback-icon {
        font-size: 1.5rem;
      }
    }
    
    /* Transcription Summary Styles */
    .transcription-summary {
      border-left: 3px solid #0d6efd;
      padding-left: 8px;
      margin-left: 4px;
    }
    
    .transcription-preview {
      background-color: #f8f9fa !important;
      border: 1px solid #e9ecef;
      font-size: 0.85rem;
      /* Removed fixed height and overflow-y to allow line-clamp to work */
      /* line-height is now handled inline for line-clamp functionality */
    }
    
    .transcription-preview:hover {
      background-color: #e9ecef !important;
    }
    
    /* Transcription text flow improvements */
    .transcription-text {
      width: 100% !important;
      white-space: normal !important;
      word-wrap: break-word !important;
      overflow-wrap: break-word !important;
      hyphens: auto !important;
      /* Note: display is handled inline for line-clamp functionality */
    }

    /* Force 4-line display for transcription text */
    .transcription-text[style*="-webkit-line-clamp"] {
      display: -webkit-box !important;
      -webkit-box-orient: vertical !important;
      overflow: hidden !important;
      text-overflow: ellipsis !important;
    }

    /* Ultra-specific 4-line class - highest priority with ID specificity */
    .content-card .transcription-preview .transcription-text,
    .content-card .transcription-text.force-4-lines,
    div[id*="transcription-summary"] .transcription-text {
      display: -webkit-box !important;
      -webkit-line-clamp: 4 !important;
      -webkit-box-orient: vertical !important;
      overflow: hidden !important;
      line-height: 1.6 !important;
      min-height: 88px !important;
      max-height: 88px !important;
      white-space: normal !important;
      text-overflow: ellipsis !important;
      word-wrap: break-word !important;
      font-size: 0.85rem !important;
      height: 88px !important;
    }
    
    /* Brain icon styling */
    .ai-analysis-btn .bi-brain {
      font-size: 1rem;
      color: #6f42c1;
    }
    
    .ai-analysis-btn:hover .bi-brain {
      color: #fff;
    }
    
    .ai-analysis-btn.btn-info .bi-brain {
      color: #fff;
    }
    
    /* Enhanced Status Button Styles */
    .analysis-status-btn {
      position: relative;
      overflow: hidden;
      border: 2px solid #dee2e6;
      font-weight: 600;
      font-size: 0.8rem;
      transition: all 0.3s ease;
      min-height: 32px;
    }
    
    .analysis-status-btn .progress-fill {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 0%;
      background: linear-gradient(90deg, rgba(255,255,255,0.3), rgba(255,255,255,0.1));
      transition: width 0.5s ease;
      pointer-events: none;
    }
    
    .analysis-status-btn .status-text {
      position: relative;
      z-index: 2;
    }
    
    /* Status Button States */
    .analysis-status-btn.waiting {
      background: #f8f9fa;
      color: #6c757d;
      border-color: #dee2e6;
    }
    
    .analysis-status-btn.processing {
      background: #fff3cd;
      color: #856404;
      border-color: #ffc107;
    }
    
    .analysis-status-btn.processing .progress-fill {
      background: linear-gradient(90deg, rgba(255,193,7,0.3), rgba(255,193,7,0.1));
    }
    
    .analysis-status-btn.analysed {
      background: #d1e7dd;
      color: #0f5132;
      border-color: #28a745;
    }
    
    .analysis-status-btn.incomplete {
      background: #f8d7da;
      color: #721c24;
      border-color: #dc3545;
    }
    
    .analysis-status-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

  </style>
</head>
<body>
  <!-- Header Include -->
  <%- include('../partials/header', { user, title: 'Content Management' }) %>

  <div class="container mt-5">
    <h2 class="fw-bold mb-4">Content Management</h2>

    <!-- Button to open Add Content Modal -->
    <div class="mb-3">
      <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addContentModal">
        <i class="bi bi-plus-circle"></i> Add New Content
      </button>
    </div>

    <% 
      const allTags = Array.from(new Set(
        (contentItems || []).flatMap(i => [
          ...(i.user_tags || []),
          ...(i.auto_tags || [])
        ]).filter(Boolean).map(t => t.trim())
      ));
      console.log('DEBUG: Content items count:', contentItems ? contentItems.length : 0);
    %>

    <!-- Enhanced Filter Bar -->
    <div class="card mb-4 p-3">
      <form class="row g-2 align-items-end" method="get" action="/content" id="filterForm">
        <input type="hidden" name="page" value="1">
        
        <!-- Search Filter -->
        <div class="col-md-3">
          <label for="filterSearch" class="form-label mb-0">Search</label>
          <div class="input-group">
            <input type="text" class="form-control" id="filterSearch" name="search" placeholder="Search title, summary, comments..." value="<%= search %>" autocomplete="off">
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterSearch" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>

        <!-- Tag Filter with autocomplete -->
        <div class="col-md-2">
          <label for="filterTag" class="form-label mb-0">Tag</label>
          <div class="input-group">
            <input type="text" class="form-control" id="filterTag" name="tag" placeholder="Type to filter by tag" value="<%= typeof tag !== 'undefined' ? tag : '' %>" list="tagOptions">
            <datalist id="tagOptions">
              <% allTags.forEach(function(tagOption) { %>
                <option value="<%= tagOption %>">
              <% }); %>
            </datalist>
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterTag" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>
        <!-- Date Range Filter -->
        <div class="col-md-2">
          <label for="filterFrom" class="form-label mb-0">From</label>
          <div class="input-group">
            <input type="date" class="form-control" id="filterFrom" name="from" value="<%= typeof from !== 'undefined' ? from : '' %>">
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterFrom" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>
        <div class="col-md-2">
          <label for="filterTo" class="form-label mb-0">To</label>
          <div class="input-group">
            <input type="date" class="form-control" id="filterTo" name="to" value="<%= typeof to !== 'undefined' ? to : '' %>">
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterTo" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>
        <!-- Content Type Filter -->
        <div class="col-md-2">
          <label for="filterContentType" class="form-label mb-0">Type</label>
          <div class="input-group">
            <select class="form-select" id="filterContentType" name="content_type">
              <option value="all" <%= content_type === 'all' ? 'selected' : '' %>>All Types</option>
              <option value="multimedia" <%= content_type === 'multimedia' ? 'selected' : '' %>>Multimedia</option>
              <option value="social" <%= content_type === 'social' ? 'selected' : '' %>>Social Media</option>
              <option value="files" <%= content_type === 'files' ? 'selected' : '' %>>Files</option>
              <option value="video" <%= content_type === 'video' ? 'selected' : '' %>>Video</option>
              <option value="audio" <%= content_type === 'audio' ? 'selected' : '' %>>Audio</option>
              <option value="image" <%= content_type === 'image' ? 'selected' : '' %>>Images</option>
            </select>
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterContentType" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>
        
        <!-- Status Filter -->
        <div class="col-md-2">
          <label for="filterStatus" class="form-label mb-0">Status</label>
          <div class="input-group">
            <select class="form-select" id="filterStatus" name="status">
              <option value="all" <%= status === 'all' ? 'selected' : '' %>>All Status</option>
              <option value="analyzed" <%= status === 'analyzed' ? 'selected' : '' %>>✅ Analyzed</option>
              <option value="pending" <%= status === 'pending' ? 'selected' : '' %>>⏳ Pending</option>
            </select>
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterStatus" title="Clear">
              <i class="bi bi-x-circle"></i>
            </button>
          </div>
        </div>
        
        <!-- Sort By -->
        <div class="col-md-2">
          <label for="filterSort" class="form-label mb-0">Sort by</label>
          <div class="input-group">
            <select class="form-select" id="filterSort" name="sort">
              <option value="newest" <%= sort === 'newest' ? 'selected' : '' %>>📅 Newest First</option>
              <option value="oldest" <%= sort === 'oldest' ? 'selected' : '' %>>📅 Oldest First</option>
              <option value="channel" <%= sort === 'channel' ? 'selected' : '' %>>📺 By Channel</option>
            </select>
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterSort" title="Reset to Newest">
              <i class="bi bi-arrow-clockwise"></i>
            </button>
          </div>
        </div>

        <!-- Items per page -->
        <div class="col-md-2">
          <label for="filterLimit" class="form-label mb-0">Items per page</label>
          <div class="input-group">
            <select class="form-select" id="filterLimit" name="limit">
              <option value="5" <%= (pagination && pagination.limit === 5) ? 'selected' : '' %>>5 items</option>
              <option value="10" <%= (pagination && pagination.limit === 10) ? 'selected' : '' %>>10 items</option>
              <option value="20" <%= (pagination && pagination.limit === 20) ? 'selected' : '' %>>20 items</option>
              <option value="50" <%= (pagination && pagination.limit === 50) ? 'selected' : '' %>>50 items</option>
              <option value="100" <%= (pagination && pagination.limit === 100) ? 'selected' : '' %>>100 items</option>
            </select>
            <button class="btn btn-outline-secondary clear-filter-btn" type="button" data-target="#filterLimit" title="Reset to 10">
              <i class="bi bi-arrow-clockwise"></i>
            </button>
          </div>
        </div>
        
        <div class="col-md-1">
          <button type="submit" class="btn btn-primary w-100"><i class="bi bi-funnel"></i> Filter</button>
        </div>
        <div class="col-md-1">
          <button type="button" class="btn btn-outline-secondary w-100" id="clearAllFilters">
            <i class="bi bi-x-circle"></i> Clear
          </button>
        </div>
      </form>
    </div>

    <!-- Content Cards Grid -->
    <div class="row row-cols-1 g-4" id="contentCardsGrid">
      <% if (contentItems && contentItems.length > 0) { %>
        <% contentItems.forEach(function(item) { %>
          <div class="col">
            <div class="card h-100 shadow-sm content-card flex-row flex-wrap" data-id="<%= item.id %>" data-item-type="<%= item.itemType %>" style="min-height:unset;">
              <div class="d-flex flex-row w-100">
                <!-- Enhanced Thumbnail Container -->
                <div class="thumbnail-container <%= item.sourceInfo.source === 'YouTube' || item.sourceInfo.source === 'Facebook' ? 'video-thumb' : 'image-thumb' %>">
                  <% if (item.itemType === 'file') { %>
                    <!-- File item thumbnail -->
                    <a href="/files/<%= item.metadata.fileId %>" target="_blank" rel="noopener noreferrer">
                      <% if (item.metadata && item.metadata.thumbnail) { %>
                        <img src="<%= item.metadata.thumbnail %>" alt="<%= item.displayTitle %>" 
                             data-fallback="true" data-fallback-icon="<%= item.sourceInfo.logo %>" data-fallback-color="<%= item.sourceInfo.color %>">
                      <% } else { %>
                        <i class="<%= item.sourceInfo.logo %> fallback-icon" style="color: <%= item.sourceInfo.color %>"></i>
                      <% } %>
                    </a>
                  <% } else { %>
                    <!-- Content item thumbnail -->
                    <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer">
                      <% if (item.metadata && item.metadata.thumbnail) { %>
                        <img src="<%= item.metadata.thumbnail %>" alt="Thumbnail" 
                             data-fallback="true" data-fallback-icon="<%= item.sourceInfo.logo %>" data-fallback-color="<%= item.sourceInfo.color %>"
                             >
                        <i class="<%= item.sourceInfo.logo %> fallback-icon" style="color: <%= item.sourceInfo.color %>; display: none;"></i>
                      <% } else if (item.url && item.url.match(/\.(jpg|jpeg|png|gif)$/i)) { %>
                        <img src="<%= item.url %>" alt="Thumbnail"
                             data-fallback="true" data-fallback-icon="<%= item.sourceInfo.logo %>" data-fallback-color="<%= item.sourceInfo.color %>"
                             >
                        <i class="<%= item.sourceInfo.logo %> fallback-icon" style="color: <%= item.sourceInfo.color %>; display: none;"></i>
                      <% } else { %>
                        <i class="<%= item.sourceInfo.logo %> fallback-icon" style="color: <%= item.sourceInfo.color %>"></i>
                      <% } %>
                    </a>
                  <% } %>
                </div>
                <!-- Card Body -->
                <div class="flex-grow-1 d-flex flex-row p-3 align-items-stretch">
                  <!-- Main Content (title, comment) -->
                  <div class="flex-grow-1 d-flex flex-column justify-content-between" style="min-width: 0; flex: 1 1 auto; max-width: calc(100% - 220px);">
                    <div>
                      <h5 class="card-title mb-1" title="<%= item.displayTitle %>"
                        data-bs-toggle="modal" data-bs-target="#viewContentModal" data-id="<%= item.id %>">
                        <%= item.displayTitle %>
                      </h5>
                      <% const comment = item.user_comments || ''; %>
                      <p class="card-text mb-2 text-muted small" style="white-space:normal; overflow-wrap:break-word;" title="<%= comment %>">
                        <%= comment.length > 200 ? comment.slice(0, 200) + '...' : comment %>
                        <% if (comment.length > 200) { %>
                          <a href="#" class="show-full-comment" data-id="<%= item.id %>">Show more</a>
                        <% } %>
                      </p>
                      
                      <!-- Transcription Summary -->
                      <% if (item.summary) { %>
                      <div class="transcription-summary mb-2" id="transcription-summary-<%= item.id %>">
                        <div class="position-relative">
                          <div class="transcription-preview bg-light p-2 rounded" style="font-size: 0.85rem; width: 100%; min-width: 250px;">
                            <div class="transcription-text text-dark force-4-lines" style="height: 88px !important; min-height: 88px !important; max-height: 88px !important; line-height: 1.6 !important; font-size: 0.85rem !important; overflow: hidden !important; white-space: normal !important; word-wrap: break-word !important; position: relative;">
                              <span class="text-content" style="display: block; overflow: hidden;">
                                <%= item.summary %>
                              </span>
                            </div>
                          </div>
                          <button class="btn btn-sm btn-outline-primary position-absolute top-0 end-0 mt-1 me-1 py-0 px-2 copy-summary-btn" data-content-id="<%= item.id %>" title="Copy Summary" style="z-index: 10;">
                            <i class="bi bi-clipboard" style="font-size: 0.7rem;"></i>
                          </button>
                        </div>
                      </div>
                      <% } else { %>
                      <div class="transcription-summary mb-2" id="transcription-summary-<%= item.id %>" style="display: none;">
                        <div class="position-relative">
                          <div class="transcription-preview bg-light p-2 rounded small">
                            <div class="transcription-text text-muted force-4-lines" style="height: 88px !important; min-height: 88px !important; max-height: 88px !important; line-height: 1.6 !important; font-size: 0.85rem !important; overflow: hidden !important; white-space: normal !important; word-wrap: break-word !important; position: relative;">
                              <span class="text-content" style="display: block; overflow: hidden;">Loading transcription...</span>
                            </div>
                          </div>
                          <button class="btn btn-sm btn-outline-primary position-absolute top-0 end-0 mt-1 me-1 py-0 px-2 copy-summary-btn" data-content-id="<%= item.id %>" title="Copy Summary" style="z-index: 10;">
                            <i class="bi bi-clipboard" style="font-size: 0.7rem;"></i>
                          </button>
                        </div>
                      </div>
                      <% } %>
                    </div>
                    <!-- Tags and Sentiment -->
                    <div class="mt-auto">
                      <!-- Tags -->
                      <div class="d-flex flex-wrap gap-1 align-items-center">
                        <% 
                          let tags = [];
                          // Handle auto_tags - ensure it's an array and not null/undefined
                          if (item.auto_tags && Array.isArray(item.auto_tags) && item.auto_tags.length > 0) {
                            tags = tags.concat(item.auto_tags.map(t => ({ tag: t, type: 'auto' })));
                          }
                          // Handle user_tags - ensure it's an array and not null/undefined  
                          if (item.user_tags && Array.isArray(item.user_tags) && item.user_tags.length > 0) {
                            tags = tags.concat(item.user_tags.map(t => ({ tag: t, type: 'user' })));
                          }
                          let shownTags = tags.slice(0, 4);
                          let moreTags = tags.length > 4 ? tags.length - 4 : 0;
                          let hiddenTags = tags.slice(4);
                        %>
                        <% shownTags.forEach(function(t) { %>
                          <span class="badge <%= t.type === 'auto' ? 'bg-info' : 'bg-success' %> tag-badge" data-tag="<%= t.tag %>">
                            <%= t.tag %>
                          </span>
                        <% }); %>
                        <% if (moreTags) { %>
                          <span class="badge bg-secondary more-tags-badge" style="cursor: pointer;" 
                                data-bs-toggle="modal" data-bs-target="#allTagsModal" 
                                data-content-id="<%= item.id %>"
                                data-all-tags="<%= encodeURIComponent(JSON.stringify(tags)) %>"
                                data-content-title="<%= (item.generated_title || item.title || 'Content').replace(/"/g, '&quot;') %>"
                                title="Click to see all tags">
                            +<%= moreTags %> more
                          </span>
                        <% } %>
                        
                        <!-- Sentiment Badge (moved from summary section) -->
                        <% if (item.sentiment) { %>
                          <% 
                            let sentimentData = item.sentiment;
                            if (typeof sentimentData === 'string') {
                              try {
                                sentimentData = JSON.parse(sentimentData);
                              } catch (e) {
                                sentimentData = null;
                              }
                            }
                            if (sentimentData && (sentimentData.label || sentimentData.sentiment)) {
                              const sentimentLabel = sentimentData.label || sentimentData.sentiment;
                              const confidence = sentimentData.confidence || 0;
                              let badgeClass = 'bg-secondary';
                              if (sentimentLabel === 'positive') badgeClass = 'bg-success';
                              else if (sentimentLabel === 'negative') badgeClass = 'bg-danger';
                              else if (sentimentLabel === 'neutral') badgeClass = 'bg-info';
                          %>
                            <span class="badge <%= badgeClass %> small ms-2">
                              <%= sentimentLabel %> (<%= Math.round(confidence * 100) %>%)
                            </span>
                          <% } %>
                        <% } %>
                      </div>
                    </div>
                  </div>
                  <!-- Right Info Column -->
                  <div class="d-flex flex-column align-items-end justify-content-between ms-3" style="min-width: 110px; max-width: 120px;">
                    <div class="text-end">
                      <div class="d-flex align-items-center justify-content-end mb-1">
                        <i class="bi <%= item.sourceInfo.logo %> me-1" style="color: <%= item.sourceInfo.color %>; font-size: 1rem;"></i>
                        <span class="fw-semibold small"><%= item.sourceInfo.source %></span>
                      </div>

                      <div class="text-muted small"><%= item.createdAt ? new Date(item.createdAt).toLocaleDateString() : '' %></div>
                      
                      <!-- Enhanced Status Button -->
                      <div class="mt-2">
                        <button class="btn btn-sm w-100 analysis-status-btn" 
                                id="status-btn-<%= item.id %>" 
                                data-id="<%= item.id %>" 
                                data-item-type="<%= item.itemType %>"
                                data-bs-toggle="modal" 
                                data-bs-target="#analysisProgressModal">
                          <span class="status-text">Checking...</span>
                          <div class="progress-fill"></div>
                        </button>
                      </div>
                    </div>
                    
                    <!-- Compact Action Buttons -->
                    <div class="d-flex flex-column gap-1 mt-2">
                      <div class="d-flex gap-1">
                        <a href="#" class="btn btn-outline-primary btn-sm edit-content-btn" data-id="<%= item.id %>" data-bs-toggle="modal" data-bs-target="#editContentModal" title="Edit">
                          <i class="bi bi-pencil" style="font-size: 0.8rem;"></i>
                        </a>
                        <% if (item.itemType === 'file') { %>
                          <a href="/files/<%= item.id %>/analysis/view" class="btn btn-outline-info btn-sm" title="View AI Analysis">
                            <i class="bi bi-brain" style="font-size: 0.8rem;"></i>
                          </a>
                        <% } else { %>
                          <a href="/content/<%= item.id %>/analysis/view" class="btn btn-outline-info btn-sm" title="View AI Analysis">
                            <i class="bi bi-brain" style="font-size: 0.8rem;"></i>
                          </a>
                        <% } %>
                        <button class="btn btn-outline-warning btn-sm reprocess-content-btn" data-id="<%= item.id %>" data-item-type="<%= item.itemType %>" title="Reprocess AI Analysis">
                          <i class="bi bi-arrow-clockwise" style="font-size: 0.8rem;"></i>
                        </button>
                      </div>
                      <div class="d-flex gap-1">
                        <button class="btn btn-outline-success btn-sm share-content-btn" data-id="<%= item.id %>" title="Share">
                          <i class="bi bi-share" style="font-size: 0.8rem;"></i>
                        </button>
                        <button class="btn btn-outline-danger btn-sm delete-content-btn" data-id="<%= item.id %>" data-item-type="<%= item.itemType %>" title="Delete">
                          <i class="bi bi-trash" style="font-size: 0.8rem;"></i>
                        </button>
                      </div>
                    </div>
                    
                    <div class="mt-1">
                      <input type="checkbox" class="form-check-input content-select-checkbox" data-id="<%= item.id %>">
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div class="col">
          <div class="alert alert-info">No content found. Click "Add New Content" to get started!</div>
        </div>
      <% } %>
    </div>

    <!-- Enhanced Pagination -->
    <% if (pagination && pagination.totalPages > 1) { %>
      <div class="row mt-4">
        <div class="col-12">
          <div class="d-flex justify-content-between align-items-center">
            <!-- Items per page info -->
            <div class="text-muted">
              Showing <%= Math.min(pagination.limit * (pagination.currentPage - 1) + 1, pagination.totalItems) %>-<%= Math.min(pagination.limit * pagination.currentPage, pagination.totalItems) %> of <%= pagination.totalItems %> items
            </div>
            
            <!-- Pagination controls -->
            <nav aria-label="Content pagination">
              <ul class="pagination mb-0">
                <!-- Previous button -->
                <li class="page-item <%= !pagination.hasPrev ? 'disabled' : '' %>">
                                     <a class="page-link" href="<%= pagination.hasPrev ? pagination.buildUrl(pagination.prevPage) : '#' %>" 
                     aria-label="Previous" <%= !pagination.hasPrev ? 'tabindex="-1"' : '' %>>
                    <span aria-hidden="true">&laquo;</span>
                  </a>
                </li>
                
                <!-- Page numbers -->
                <% 
                let startPage = Math.max(1, pagination.currentPage - 2);
                let endPage = Math.min(pagination.totalPages, pagination.currentPage + 2);
                
                // Adjust if we're near the beginning
                if (pagination.currentPage <= 3) {
                  endPage = Math.min(5, pagination.totalPages);
                }
                
                // Adjust if we're near the end
                if (pagination.currentPage >= pagination.totalPages - 2) {
                  startPage = Math.max(1, pagination.totalPages - 4);
                }
                %>
                
                <% if (startPage > 1) { %>
                  <li class="page-item">
                    <a class="page-link" href="?page=1<%= tag ? '&tag=' + encodeURIComponent(tag) : '' %><%= from ? '&from=' + from : '' %><%= to ? '&to=' + to : '' %>">1</a>
                  </li>
                  <% if (startPage > 2) { %>
                    <li class="page-item disabled">
                      <span class="page-link">...</span>
                    </li>
                  <% } %>
                <% } %>
                
                <% for (let i = startPage; i <= endPage; i++) { %>
                  <li class="page-item <%= i === pagination.currentPage ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %><%= tag ? '&tag=' + encodeURIComponent(tag) : '' %><%= from ? '&from=' + from : '' %><%= to ? '&to=' + to : '' %>">
                      <%= i %>
                      <% if (i === pagination.currentPage) { %>
                        <span class="visually-hidden">(current)</span>
                      <% } %>
                    </a>
                  </li>
                <% } %>
                
                <% if (endPage < pagination.totalPages) { %>
                  <% if (endPage < pagination.totalPages - 1) { %>
                    <li class="page-item disabled">
                      <span class="page-link">...</span>
                    </li>
                  <% } %>
                  <li class="page-item">
                    <a class="page-link" href="?page=<%= pagination.totalPages %><%= tag ? '&tag=' + encodeURIComponent(tag) : '' %><%= from ? '&from=' + from : '' %><%= to ? '&to=' + to : '' %>"><%= pagination.totalPages %></a>
                  </li>
                <% } %>
                
                <!-- Next button -->
                <li class="page-item <%= !pagination.hasNext ? 'disabled' : '' %>">
                  <a class="page-link" href="<%= pagination.hasNext ? `?page=${pagination.nextPage}${tag ? '&tag=' + encodeURIComponent(tag) : ''}${from ? '&from=' + from : ''}${to ? '&to=' + to : ''}` : '#' %>" 
                     aria-label="Next" <%= !pagination.hasNext ? 'tabindex="-1"' : '' %>>
                    <span aria-hidden="true">&raquo;</span>
                  </a>
                </li>
              </ul>
            </nav>
          </div>
        </div>
      </div>
    <% } %>
  </div>

  <!-- Add Content Modal -->
  <div class="modal fade" id="addContentModal" tabindex="-1" aria-labelledby="addContentModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
      <form class="modal-content" id="addContentForm" action="/content" method="POST" enctype="multipart/form-data">
        <div class="modal-header">
          <h5 class="modal-title" id="addContentModalLabel">Add New Content</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <!-- Content Type Toggle -->
          <div class="mb-4">
            <div class="btn-group w-100" role="group" aria-label="Content type selection">
              <input type="radio" class="btn-check" name="contentTypeToggle" id="singleUrlContentType" checked>
              <label class="btn btn-outline-primary" for="singleUrlContentType">
                <i class="bi bi-link-45deg"></i> Single URL
              </label>
              
              <input type="radio" class="btn-check" name="contentTypeToggle" id="bulkUrlContentType">
              <label class="btn btn-outline-primary" for="bulkUrlContentType">
                <i class="bi bi-list-ul"></i> Bulk URLs
              </label>
              
              <input type="radio" class="btn-check" name="contentTypeToggle" id="fileContentType">
              <label class="btn btn-outline-primary" for="fileContentType">
                <i class="bi bi-cloud-upload"></i> Upload Files
              </label>
            </div>
          </div>

          <!-- Single URL Content Section -->
          <div id="singleUrlContentSection">
            <!-- Title Input -->
            <div class="mb-3">
              <label for="contentTitle" class="form-label">Title (Optional)</label>
              <input type="text" class="form-control" id="contentTitle" name="title" placeholder="Content Title (will be auto-generated if empty)">
            </div>
            <!-- URL Input -->
            <div class="mb-3">
              <label for="contentUrl" class="form-label">Content URL</label>
              <input type="url" class="form-control" id="contentUrl" name="url" placeholder="https://example.com/post">
            </div>
          </div>

          <!-- Bulk URL Content Section -->
          <div id="bulkUrlContentSection" style="display: none;">
            <div class="mb-3">
              <label for="bulkContentUrls" class="form-label">
                <i class="bi bi-list-ul"></i> Multiple URLs (one per line)
              </label>
              <textarea class="form-control" id="bulkContentUrls" name="bulk_urls" rows="15" 
                        style="min-height: 300px; max-height: 400px; resize: vertical; overflow-y: auto; font-family: 'Courier New', monospace; line-height: 1.4;"
                        placeholder="https://example.com/post1
https://example.com/post2
https://example.com/post3
https://youtube.com/watch?v=example
https://instagram.com/p/example/
https://twitter.com/user/status/123
https://linkedin.com/posts/activity-456
https://facebook.com/posts/789
https://tiktok.com/@user/video/123
https://pinterest.com/pin/456

Paste multiple URLs here, one per line.
Blank lines will be ignored.
You can add as many URLs as needed - the field will scroll automatically."></textarea>
              <div class="form-text">
                <i class="bi bi-info-circle"></i> 
                <strong>Tip:</strong> Paste multiple URLs, one per line. Empty lines and duplicates will be automatically filtered out. 
                The field will scroll when you have many URLs.
              </div>
            </div>
            <!-- URL Preview Counter -->
            <div id="urlPreviewSection" class="mb-3" style="display: none;">
              <div class="alert alert-info">
                <i class="bi bi-check-circle"></i> 
                <span id="urlCount">0</span> valid URLs detected
                <button type="button" class="btn btn-sm btn-outline-primary ms-2" id="previewUrlsBtn">
                  <i class="bi bi-eye"></i> Preview
                </button>
              </div>
              <div id="urlPreviewList" class="d-none">
                <!-- URL preview list will be populated by JavaScript -->
              </div>
            </div>
          </div>

          <!-- File Upload Section -->
          <div id="fileContentSection" style="display: none;">
            <!-- Upload Method Toggle -->
            <div class="mb-3">
              <div class="btn-group" role="group" aria-label="File upload method">
                <input type="radio" class="btn-check" name="fileUploadMethod" id="fileSelectMethod" checked>
                <label class="btn btn-outline-secondary btn-sm" for="fileSelectMethod">
                  <i class="bi bi-folder-open"></i> Select Files
                </label>
                
                <input type="radio" class="btn-check" name="fileUploadMethod" id="filePathMethod">
                <label class="btn btn-outline-secondary btn-sm" for="filePathMethod">
                  <i class="bi bi-terminal"></i> File Paths
                </label>
              </div>
            </div>

            <!-- File Selection Method -->
            <div id="fileSelectSection">
              <div class="mb-3">
                <label for="contentFiles" class="form-label">Select Files</label>
                
                <!-- Hidden file input -->
                <input type="file" class="d-none" id="contentFiles" name="files" multiple 
                       accept="image/*,audio/*,video/*,.pdf,.doc,.docx,.txt">
                
                <!-- Custom file upload area -->
                <div class="file-upload-area border-2 border-dashed rounded p-4 text-center" 
                     id="fileUploadArea" 
                     style="border-color: #dee2e6; background-color: #f8f9fa; cursor: pointer; transition: all 0.3s ease;">
                  <div class="file-upload-content">
                    <i class="bi bi-cloud-upload-fill text-primary mb-3" style="font-size: 3rem;"></i>
                    <h5 class="mb-2">Click to Select Files</h5>
                    <p class="text-muted mb-2">or drag and drop files here</p>
                    <button type="button" class="btn btn-primary" id="selectFilesBtn">
                      <i class="bi bi-folder-open"></i> Browse Files
                    </button>
                    <div class="mt-3">
                      <small class="text-muted">
                        <i class="bi bi-info-circle"></i> 
                        Supported: Images, Audio, Video, PDF, Word documents, Text files. Max 1GB per file.
                      </small>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- File Path Method -->
            <div id="filePathSection" style="display: none;">
              <div class="mb-3">
                <label for="bulkFilePaths" class="form-label">
                  <i class="bi bi-terminal"></i> File Paths (one per line)
                </label>
                <textarea class="form-control" id="bulkFilePaths" name="bulk_file_paths" rows="15" 
                          style="min-height: 300px; max-height: 400px; resize: vertical; overflow-y: auto; font-family: 'Courier New', monospace; line-height: 1.4;"
                          placeholder="/Users/username/Documents/image1.jpg
/Users/username/Downloads/video.mp4
/Users/username/Pictures/photo.png
/Users/username/Movies/video.mov
/home/user/media/audio.mp3
/home/user/documents/file.pdf
\\server\share\media\video.avi
\\server\photos\vacation\img001.jpg
/mnt/storage/archive/document.docx
/var/media/uploads/presentation.pptx

Paste full file paths here, one per line.
Files must be accessible to the server.
You can add as many file paths as needed - the field will scroll automatically."></textarea>
                <div class="form-text">
                  <i class="bi bi-exclamation-triangle text-warning"></i> 
                  <strong>Note:</strong> File paths must be accessible to the server. This works best for server-local files or network-mounted drives.
                  The field will scroll when you have many file paths.
                </div>
              </div>
              <!-- Path Preview Counter -->
              <div id="pathPreviewSection" class="mb-3" style="display: none;">
                <div class="alert alert-info">
                  <i class="bi bi-check-circle"></i> 
                  <span id="pathCount">0</span> file paths detected
                  <button type="button" class="btn btn-sm btn-outline-primary ms-2" id="previewPathsBtn">
                    <i class="bi bi-eye"></i> Preview
                  </button>
                </div>
                <div id="pathPreviewList" class="d-none">
                  <!-- Path preview list will be populated by JavaScript -->
                </div>
              </div>
            </div>

            <!-- Selected Files Preview (for file selection method) -->
                      <div id="selectedFilesPreview" class="mb-3" style="display: none;">
            <label class="form-label">Selected Files:</label>
            <div id="filesList" class="border rounded p-2 bg-light" style="max-height: 200px; overflow-y: auto;">
              <!-- File list will be populated by JavaScript -->
            </div>
          </div>
          </div>

          <!-- Common Fields (for both URL and File content) -->
          <div class="border-top pt-3">
            <!-- User Comment -->
            <div class="mb-3">
              <label for="contentComment" class="form-label">Your Comment</label>
              <textarea class="form-control" id="contentComment" name="user_comments" rows="2" 
                        placeholder="Add your thoughts, notes, or description..."></textarea>
            </div>

            <!-- User Tags -->
            <div class="mb-3">
              <label for="contentTags" class="form-label">Tags (comma separated)</label>
              <input type="text" class="form-control" id="contentTags" name="user_tags" placeholder="tag1, tag2, tag3">
              <div class="form-text">Tags help organize and find your content later</div>
            </div>

            <!-- Group Assignment -->
            <div class="mb-3">
              <label for="contentGroups" class="form-label">Assign to Groups (Optional)</label>
              <select class="form-select" id="contentGroups" name="group_ids" multiple>
                <% (contentGroups || []).forEach(function(group) { %>
                  <option value="<%= group.id %>"><%= group.name %></option>
                <% }); %>
              </select>
              <div class="form-text">Hold Ctrl/Cmd to select multiple groups</div>
            </div>
          </div>

          <!-- Progress Bar (for file uploads) -->
          <div id="uploadProgressSection" class="mb-3" style="display: none;">
            <label class="form-label">Upload Progress</label>
            <div class="progress">
              <div class="progress-bar" role="progressbar" style="width: 0%" id="uploadProgressBar">0%</div>
            </div>
          </div>

          <div id="addContentAlert" class="alert d-none mt-2"></div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-primary" id="addContentBtn">
            <span id="singleUrlBtnText"><i class="bi bi-plus-circle"></i> Add Content</span>
            <span id="bulkUrlBtnText" style="display: none;"><i class="bi bi-list-check"></i> Import URLs</span>
            <span id="fileBtnText" style="display: none;"><i class="bi bi-cloud-upload"></i> Upload Files</span>
            <span id="pathBtnText" style="display: none;"><i class="bi bi-terminal"></i> Import Paths</span>
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Edit Content Modal -->
  <div class="modal fade" id="editContentModal" tabindex="-1" aria-labelledby="editContentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <form class="modal-content" id="editContentForm">
        <input type="hidden" id="editContentId" name="id">
        <div class="modal-header">
          <h5 class="modal-title" id="editContentModalLabel">Edit Content</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <!-- Title Input -->
          <div class="mb-3">
            <label for="editContentTitle" class="form-label">Title</label>
            <input type="text" class="form-control" id="editContentTitle" name="title" placeholder="Content Title">
          </div>
          <!-- URL Input (readonly) -->
          <div class="mb-3">
            <label for="editContentUrl" class="form-label">Content URL</label>
            <input type="url" class="form-control" id="editContentUrl" name="url" readonly>
          </div>
          <!-- User Comment -->
          <div class="mb-3">
            <label for="editContentComment" class="form-label">Your Comment</label>
            <textarea class="form-control" id="editContentComment" name="user_comments" rows="2"></textarea>
          </div>




          <!-- User Tags -->
          <div class="mb-3">
            <label for="editContentTags" class="form-label">Tags (comma separated)</label>
            <input type="text" class="form-control" id="editContentTags" name="user_tags" placeholder="tag1, tag2">
          </div>
          <!-- Group Assignment -->
          <div class="mb-3">
            <label for="editContentGroups" class="form-label">Assign to Groups</label>
            <select class="form-select" id="editContentGroups" name="group_ids" multiple>
              <% (contentGroups || []).forEach(function(group) { %>
                <option value="<%= group.id %>"><%= group.name %></option>
              <% }); %>
            </select>
            <div class="form-text">Hold Ctrl/Cmd to select multiple groups.</div>
          </div>
          <div id="editContentAlert" class="alert d-none mt-2"></div>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save Changes</button>
        </div>
      </form>
    </div>
  </div>

  <!-- View Content Modal (placeholder, to be filled by JS) -->
  <div class="modal fade" id="viewContentModal" tabindex="-1" aria-labelledby="viewContentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="viewContentModalLabel">Content Details</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body" id="viewContentModalBody">
          <!-- Content details will be filled by JS -->
        </div>
      </div>
    </div>
  </div>

  <!-- All Tags Modal -->
  <div class="modal fade" id="allTagsModal" tabindex="-1" aria-labelledby="allTagsModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="allTagsModalLabel">All Tags</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body" id="allTagsModalBody">
          <!-- Tags will be populated by JavaScript -->
        </div>
      </div>
    </div>
  </div>



  <!-- Footer Include -->
  <%- include('../partials/footer') %>
  <!-- jQuery (required for Bootstrap Select) -->
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


  <!-- Custom JS for Content Management -->
  <script src="/js/content-management.js?v=<%= Date.now() %>"></script>
  <script src="/js/content-filters.js?v=<%= Date.now() %>"></script>
  <script src="/js/ai-analysis.js?v=<%= Date.now() %>"></script>
  <script src="/js/content-tags-modal.js?v=<%= Date.now() %>"></script>
  
  <!-- Content Upload Toggle for Add Content Modal -->
  <script src="/js/content-upload-toggle.js?v=<%= Date.now() %>"></script>
  
  <!-- Content List Enhancements moved to external file for CSP compliance -->
  <script src="/js/content-list-enhancements.js?v=<%= Date.now() %>"></script>
  
  <!-- Enhanced Status Button System -->
  <script src="/js/status-buttons.js?v=<%= Date.now() %>"></script>
  
  <!-- Content Reprocessing -->
  <script src="/js/content-reprocess.js?v=<%= Date.now() %>"></script>
  
  <!-- Form Handlers -->
  <script src="/js/form-handlers.js?v=<%= Date.now() %>"></script>
  
  <!-- Image Error Handlers -->
  <script src="/js/image-error-handlers.js?v=<%= Date.now() %>"></script>
  
  <style>
    /* ✨ FIXED: Restore proper 4-line display for transcription/summary text */
    body .container .content-card .transcription-summary .transcription-preview .force-4-lines,
    .content-card .transcription-preview .transcription-text.force-4-lines,
    .force-4-lines[style] {
      /* Essential flexbox setup for line-clamp */
      display: -webkit-box !important;
      -webkit-box-orient: vertical !important;
      -webkit-line-clamp: 4 !important;
      
      /* Size and spacing */
      height: 88px !important;
      min-height: 88px !important;
      max-height: 88px !important;
      line-height: 1.6 !important;
      font-size: 0.85rem !important;
      
      /* Overflow handling */
      overflow: hidden !important;
      text-overflow: ellipsis !important;
      white-space: normal !important;
      word-wrap: break-word !important;
    }
    
    body .container .content-card .transcription-summary .transcription-preview .force-4-lines .text-content,
    .content-card .transcription-preview .transcription-text.force-4-lines .text-content,
    .force-4-lines .text-content[style] {
      /* Inherit parent flexbox behavior for proper line-clamp */
      display: inherit !important;
      line-height: inherit !important;
      font-size: inherit !important;
      height: auto !important;
      overflow: inherit !important;
      /* Let parent handle line-clamp */
      -webkit-box-orient: unset !important;
    }
  </style>

  <!-- Analysis Progress Modal -->
  <div class="modal fade" id="analysisProgressModal" tabindex="-1" aria-labelledby="analysisProgressModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="analysisProgressModalLabel">AI Analysis Progress</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div id="progressModalContent">
            <div class="text-center py-4">
              <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
              </div>
              <p class="mt-2">Loading analysis progress...</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>



</body>
</html> 