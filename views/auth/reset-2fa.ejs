<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .reset-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            max-width: 600px;
            width: 100%;
        }
        .reset-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            background: linear-gradient(45deg, #f39c12, #e74c3c);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2rem;
        }
        .btn-reset {
            background: linear-gradient(45deg, #f39c12, #e74c3c);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 12px 24px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-reset:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(243, 156, 18, 0.3);
            background: linear-gradient(45deg, #e67e22, #c0392b);
            color: white;
        }
        .warning-box {
            background: rgba(243, 156, 18, 0.1);
            border: 1px solid rgba(243, 156, 18, 0.2);
            border-radius: 12px;
            padding: 1rem;
        }
        .info-box {
            background: rgba(13, 202, 240, 0.1);
            border: 1px solid rgba(13, 202, 240, 0.2);
            border-radius: 12px;
            padding: 1rem;
        }
        .back-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }
        .back-link:hover {
            color: #5a6fd8;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">DaySave.app</a>
            <div class="d-flex ms-auto">
                <a href="/auth/login" class="btn btn-outline-primary">Back to Login</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="reset-card p-4 p-md-5">
                    <!-- Icon -->
                    <div class="text-center mb-4">
                        <div class="reset-icon">
                            <i class="fas fa-exclamation-triangle"></i>
                        </div>
                        <h2 class="fw-bold text-dark mb-2">Reset Two-Factor Authentication</h2>
                        <p class="text-muted">Request admin assistance to reset your 2FA</p>
                    </div>

                    <!-- Error Messages -->
                    <% if (error) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            <%= error %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    <% } %>

                    <!-- Success Messages -->
                    <% if (success) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle me-2"></i>
                            <%= success %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    <% } %>

                    <!-- Warning Box -->
                    <div class="warning-box mb-4">
                        <h6 class="text-warning mb-2">
                            <i class="fas fa-exclamation-triangle me-2"></i>Important Notice
                        </h6>
                        <ul class="small mb-0 text-muted">
                            <li>This will disable your current 2FA setup</li>
                            <li>You'll need to set up 2FA again after reset</li>
                            <li>Admin review is required for security</li>
                            <li>Process may take 24-48 hours</li>
                        </ul>
                    </div>

                    <!-- Reset Request Form -->
                    <form action="/auth/reset-2fa" method="POST" class="mb-4" id="reset2faForm">
                        <div class="mb-3">
                            <label for="email" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="email" name="email" required 
                                   placeholder="Enter your account email address" autocomplete="email">
                            <small class="text-muted">Must match your account email</small>
                        </div>
                        
                        <div class="mb-4">
                            <label for="reason" class="form-label">Reason for Reset Request</label>
                            <textarea class="form-control" id="reason" name="reason" rows="4" required 
                                      placeholder="Please explain why you need to reset your 2FA (e.g., lost phone, new device, app issues)" 
                                      minlength="10"></textarea>
                            <small class="text-muted">Minimum 10 characters. Be specific to help admins process your request.</small>
                        </div>
                        
                        <button type="submit" class="btn btn-reset w-100" id="resetButton">
                            <i class="fas fa-paper-plane me-2"></i>Submit Reset Request
                        </button>
                    </form>

                    <!-- Information Box -->
                    <div class="info-box mb-4">
                        <h6 class="text-info mb-2">
                            <i class="fas fa-info-circle me-2"></i>What happens next?
                        </h6>
                        <ol class="small mb-0 text-muted">
                            <li>Your request is logged and sent to administrators</li>
                            <li>Admin will verify your identity and request details</li>
                            <li>If approved, your 2FA will be reset</li>
                            <li>You'll receive an email notification</li>
                            <li>You can then log in and set up 2FA again</li>
                        </ol>
                    </div>

                    <!-- Alternative Options -->
                    <div class="text-center">
                        <p class="text-muted mb-3">Try these options first:</p>
                        <div class="d-flex justify-content-center gap-3 flex-wrap">
                            <a href="/auth/verify-2fa" class="back-link">
                                <i class="fas fa-mobile-alt me-2"></i>Use Backup Code
                            </a>
                            <a href="/auth/login" class="back-link">
                                <i class="fas fa-arrow-left me-2"></i>Back to Login
                            </a>
                            <a href="/support" class="back-link">
                                <i class="fas fa-life-ring me-2"></i>Contact Support
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- External JavaScript for 2FA reset form (CSP compliant) -->
    <script src="/js/reset-2fa.js?v=<%= Date.now() %>"></script>
</body>
</html>
